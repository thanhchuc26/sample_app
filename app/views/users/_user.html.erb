<li>
  <%= gravatar_for user, size: Settings.user.avatar_size %>
  <%= link_to user.name, user %>
  <% if delete_user? user %>
    <%= link_to t(".delete"), user, method: :delete,
                                    data: {confirm: t("users.destroy.confirm_delete")} %>
  <% end %>
</li>
