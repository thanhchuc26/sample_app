<%= link_to gravatar_for(current_user,
   size: Settings.user.avatar_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("users.show.view_profile"), current_user %></span>
<span>
  <%= t("micropost.create.number_post", count: current_user.microposts.count) %>
</span>
