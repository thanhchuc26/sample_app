user:
  name:
    max_length: 50
  email:
    max_length: 100
    regex: !ruby/regexp /\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i
  password:
    min_length: 6
  avatar_size: 50
  paginate:
    per_page: 10
  time_reset_expire: 2
micropost:
  content:
    max_length: 140
  pic_size: 5
  img_format: ["image/jpeg", "image/gif", "image/png"]
relationship:
  size: 30
